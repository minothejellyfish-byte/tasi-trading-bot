# Auto-fix Report — 2026-05-30 23:13 Riyadh

## Issue
TASI pre-market screener crashed at ~22:09 with `KeyError: 'ticker'` at `format_pick()`.

## Root Cause
- `screener.py` was updated to write `"ticker"` in `picks.json`, but the old compiled `.pyc` (pre-update) was still running at 22:09.
- It wrote `picks.json` with `"symbol"` keys.
- `format_pick()` expected `"ticker"` → crash.

## Fixes Applied
1. Migrated `picks.json`: `symbol` → `ticker`, missing fields backfilled with `null`.
2. Verified `.pyc` is now fresh (22:53) — future runs will write `"ticker"` correctly.

## Current Picks (5)
| # | Ticker | Score |
|---|--------|-------|
| 1 | 4280.SR | 81.3 |
| 2 | 2280.SR | 58.9 |
| 3 | 7010.SR | 50.6 |
| 4 | 1150.SR | 49.9 |
| 5 | 4250.SR | 48.1 |

## Note
These picks lack `rsi`, `close`, `trend_pct`, etc. because the old code didn't persist them.
Next premarket run (Sun 09:50) will have full fields.

## Action for Amin
- Check if you want to manually re-run the screener now for richer data, or wait for tomorrow's cron.
